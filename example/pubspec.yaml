name: api
version: 1.0.0+1

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  convex_dart:
  fast_immutable_collections: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  collection: ^1.19.1
  flutter_lints: ^6.0.0
  faker: ^2.2.0
  recase: ^4.1.0
  convex_dart_cli:
  integration_test:
    sdk: flutter

resolution: workspace

flutter:
  config:
    enable-swift-package-manager: false
